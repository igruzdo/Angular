import { NgForObjectDirective } from './ng-for-object.directive';

describe('NgForObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new NgForObjectDirective();
    expect(directive).toBeTruthy();
  });
});
